# Runtime dependencies - what the deployed app and demo.py need.
# Training and test dependencies live in requirements-dev.txt.
#
# CPU-only PyTorch: the default PyPI wheel bundles CUDA and is several GB,
# which exceeds the build limits on free hosting tiers.
--extra-index-url https://download.pytorch.org/whl/cpu

torch>=2.1.0
numpy>=1.24.0
tiktoken>=0.5.0
streamlit>=1.30.0
